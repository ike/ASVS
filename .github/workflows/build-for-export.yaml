name: Build ASVS for Export

on:
  push:
    branches: [ github-actions ]

jobs:
  build:
    runs-on: ubuntu-latest
    options: --user root

    steps:
    - uses: actions/checkout@v3

    - name: Build
      run: |
        make all
